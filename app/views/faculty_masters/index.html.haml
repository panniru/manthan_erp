%table.table.table-condensed
  %thead
    %th Code
    %th Name
    %th Designation
  %tbody
    - @faculty.each do |faculty_master|
      %tr
        %td= link_to faculty_master.code, faculty_master
        %td= faculty_master.faculty_name
        %td= faculty_master.designation_master.name
